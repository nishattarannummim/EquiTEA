# known_faces Directory

This folder stores facial images of registered workers used for face recognition.

- Each worker has a separate subfolder named after their Worker ID.
- Images are used only to generate facial embeddings at runtime.
- **No real facial images are included** in the public repository for privacy reasons.
